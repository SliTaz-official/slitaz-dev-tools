

	* desktopbox will be split into TazDE (planned for 5.0) for autostart
	  and Yad for logoutbox to replace 'desktopbox --logout'. Other functions
	  are no longer needed since users can add icons on the desktop via LXpanel menu.
	  
	* subox to replace existing GTKdialog subox

	* Improve tazpkg-notify
	
